apiVersion: v1
kind: Service
metadata:
  name: {{ include "oas-catalogue.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "oas-catalogue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oas-catalogue.selectorLabels" . | nindent 4 }}
